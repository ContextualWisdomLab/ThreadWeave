# Direct CI, test, lint, coverage, and build dependencies.
# Keep Hatchling, coverage, and pytest synchronized with pyproject.toml.
build==1.5.0
coverage[toml]==7.15.3
hatchling==1.31.0
pytest==9.1.1
ruff==0.15.20
